<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>盛香亭 每日报表计算生成应用</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.1/css/bulma.min.css">
    <style>
      html, body {
        font-family: "Microsoft YaHei";
      }
    </style>
  </head>

  <body>
    <div class="table-container">
      <table style="text-align: center; white-space: nowrap;" class="table is-bordered">
      <thead>
          <tr>
            <% for (key of Object.keys(statement)) {%>
              <th><%= key %></th>
            <% } %>
        </tr>
      </thead>
      <tbody>
        <tr>
            <% for (value of Object.values(statement)) {%>
              <th><%= value %></th>
            <% } %>
        </tr>
      </tbody>
      </table>
    </div>
  </body>
</html>